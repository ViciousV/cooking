<%= form_for(coworker) do |f| %>
  <% if coworker.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(coworker.errors.count, "error") %> prohibited this coworker from being saved:</h2>

      <ul>
      <% coworker.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  
  <div class="field">
    <%= f.label :email %>
    <%= f.text_field :email %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
